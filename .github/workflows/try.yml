name: try
on:
  workflow_dispatch:
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: say hello to me 
      run: echo "hello"

    - name: hello to the user 
      run: echo "hi $USER "
   
